在2×n的一个长方形方格中,用一个1× 2的骨牌铺满方格,输入n ,输出铺放方案的总数. 例如n=3时,为2× 3方格，骨牌的铺放方案有三种


Input
输入包含一个整数n,表示该测试实例的长方形方格的规格是2×n (0< n<=50)。

Output
输出铺放方案的总数。

Sample
Input 
3
Output 
3

//这种图形的题还是要写数找规律，不能看题目的障眼法规律
#include<iostream>
using namespace std;
int main()
{
    int n;
    long long f[51];
    std::ios::sync_with_stdio(false);
    while (scanf("%d",&n)!=EOF) {
        f[1]=1;
        f[2]=2;
        for (int i=3;i<=n;i++) {
            f[i]=f[i-1]+f[i-2];
        }
        cout<<f[n]<<endl;
    }
    return 0;
}
